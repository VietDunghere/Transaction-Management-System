# CI-only requirements — subset of requirements.txt for fast installs.
# Excludes: lightgbm, xgboost (pull ~300MB CUDA deps on Linux),
#           optuna, imbalanced-learn (training-only, never imported by app).
# ML models are fully mocked in unit tests — these libs are not needed at test time.

# ========================================
# Core framework
# ========================================
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ========================================
# Database
# ========================================
sqlalchemy>=2.0.30
oracledb==3.4.2
alembic>=1.13.1

# ========================================
# Auth & Security
# ========================================
python-jose[cryptography]>=3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart>=0.0.9

# ========================================
# Validation & Settings
# ========================================
pydantic>=2.7.1
pydantic-settings>=2.2.1

# ========================================
# ML inference (no training, no GPU)
# ========================================
scikit-learn>=1.4.2
pandas>=2.2.2
numpy>=1.26.4
joblib>=1.4.2

# ========================================
# Logging
# ========================================
structlog>=24.1.0

# ========================================
# HTTP utilities
# ========================================
httpx>=0.27.0

# ========================================
# Testing
# ========================================
pytest>=8.2.0
pytest-asyncio>=0.23.6
